# roles/nginx/templates/nginx_vhost.conf.j2

server {
    listen 80;
    server_name ubuntu;
    root /var/www/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }
}
